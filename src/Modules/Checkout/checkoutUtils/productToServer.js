export default class ProductToServer {
  constructor(product_id, model_id, quantity, price) {
    this.product_id = product_id;
    this.model_id = model_id;
    this.quantity = quantity;
    this.price = price;
  }
}
