export default class Banner {
  constructor(id, imgUrl, title, subtitle, btnCtaContent, link) {
    this.id = id;
    this.imgUrl = imgUrl;
    this.title = title;
    this.subtitle = subtitle;
    this.btnCtaContent = btnCtaContent;
    this.link = link;
  }
}
